# xxybacktest 多品种回测改造 — 工作任务面板

> 状态：规划阶段  
> 最后更新：2026-06-05  
> 版本：v0.2（资金池共享修正版）

---

## 一、核心设计决策（已确定）

1. **Account 不持资金**：所有品种共享 `context.portfolio`（统一资金池），`add_account()` 不再传 `capital`
2. **Position 标记归属**：`Position.account_id` 字段标记持仓属于哪个品种账户
3. **规则按品种隔离**：每个 Account 持有自己的 `rule_list`、`费率配置`、`滑点配置`
4. **日志按品种隔离**：每个 Account 持有独立的 `order_list`、`trade_list`、`trade_returns`
5. **绩效只有组合级**：资金共享，分账户无法独立计算年化/夏普，只记录持仓明细/胜率/贡献
6. **向后兼容**：现有单账户策略代码零改动运行

---

## 二、改造步骤总览

| 阶段 | 步骤 | 功能点 | 涉及文件 | 风险等级 |
|------|------|--------|---------|---------|
| P1 | S1 | Account 类定义 | `objects.py` | 低 |
| P1 | S2 | Position 增加 account_id | `objects.py` | 低 |
| P1 | S3 | Context 新增 accounts / add_account | `context.py` | 中 |
| P1 | S4 | 数据层：多品种缓存共存 | `data.py` | **高** |
| P1 | S5 | 数据层：多品种懒加载 | `data.py` | 中 |
| P1 | S6 | 交易函数增加 account_id | `trading.py` | **高** |
| P1 | S7 | 规则引擎：按账户隔离 | `rules.py` | **高** |
| P1 | S8 | 回测主循环：内置事件多账户化 | `backtest.py` | **高** |
| P1 | S9 | 回测主循环：绑定函数签名更新 | `backtest.py` | 中 |
| P1 | S10 | 回测主循环：结果 DataFrame 改造 | `backtest.py` | 中 |
| P1 | S11 | 绩效计算：组合级 + 分账户明细 | `performance.py` | 中 |
| P1 | S12 | __init__.py 导出新 API | `__init__.py` | 低 |
| P1 | S13 | types.py Protocol 更新 | `types.py` | 低 |
| P2 | S14 | simulation/runner.py 多账户结果保存 | `simulation/runner.py` | 中 |
| P2 | S15 | simulation/submitter.py 子账户配置 | `simulation/submitter.py` | 低 |
| P2 | S16 | 实盘层：live/context.py 多账户 | `live/context.py` | 中 |
| P2 | S17 | 实盘层：live/trading.py account_id | `live/trading.py` | 中 |
| P2 | S18 | 实盘层：live/runner.py 绑定 | `live/runner.py` | 中 |
| P2 | S19 | 实盘层：live/recorder.py 存储 | `live/recorder.py` | 低 |
| P3 | S20 | tests/ 测试用例更新 | `tests/` | 中 |
| P3 | S21 | web/ API 路由扩展 | `web/routes/` | 低 |

---

## 三、各步骤详细说明

### P1-S1：Account 类定义
**文件**：`xxybacktest/objects.py`

**功能**：新增 `Account` 类，包含：
- `account_id`, `asset_type`
- 费率配置（open_tax, close_tax, commission 等）
- 滑点配置（slip, sliptype）
- 规则链（rule_list）
- 成交量限制（order_volume_ratio）
- 独立日志（order_list, trade_list, trade_returns）
- 独立绩效占位（returns, position_ratio, position_snapshots, win, trade_num）

**矛盾点**：原 `context.account` 已作为费率配置字段存在（见 `context.py:153-163`），命名冲突。新 Account 类命名建议改为 `AssetAccount` 或保持 `Account` 但修改 `context.account` 的属性代理逻辑。

**遗漏点**：`Account` 类需要 `__getstate__` / `__setstate__` 支持序列化（context.g 的持久化依赖此机制）。

---

### P1-S2：Position 增加 account_id
**文件**：`xxybacktest/objects.py`

**功能**：
```python
class Position:
    def __init__(self, code, amount, enable_amount, last_sale_price, account_id="default"):
        # ... existing fields ...
        self.account_id = account_id
```

**矛盾点**：所有创建 Position 的位置都需要更新构造函数调用（5 处）：
- `trading.py:71-79`（order_buy 新建持仓）
- `trading.py:91-100`（live/trading.py 从 QMT 构建）
- `live/context.py:44-54`（create_live_context 从 QMT 构建）
- `backtest.py:155-183`（_before_market 送股后修改 amount，不创建新对象，但需确保 account_id 不丢失）

**遗漏点**：无

---

### P1-S3：Context 新增 accounts / add_account
**文件**：`xxybacktest/context.py`

**功能**：
1. `create_context()` 中新增字段：
   - `accounts = {}`（DictObj 内新增 key）
   - `main_account_id = "default"`
2. 新增 `add_account(account_id, asset_type, order_cost, slippage)` 函数
3. 向后兼容的属性代理（保留 `context.portfolio` 等快捷访问）

**矛盾点**：
- `create_context()` 返回的是 `DictObj`，DictObj 的 `__getattr__` 直接读 `_attributes` dict。如果 `accounts` 中存的是普通 Python 类（Account）而非 DictObj，则 `context.accounts.fund.rule_list` 可以工作（因为 Account 是普通类），但 `context.accounts["fund"]["rule_list"]` 不能工作（因为 Account 不支持 `__getitem__`）。
- 如果 Account 也继承 DictObj，则嵌套递归序列化可能出问题。建议 Account 保持为普通类，只通过属性访问。

**遗漏点**：
- 需要新增 `get_account(account_id=None)` 辅助函数
- `context.account`（原费率配置）与新 Account 类命名冲突，需要处理：
  - 方案 A：`context.account` 保持为费率代理（指向 `accounts["default"]` 的费率字段），Account 类改名为 `AssetAccount`
  - 方案 B：`context.fee` 作为新的费率快捷访问，`context.account` 做 deprecated 兼容别名

---

### P1-S4：数据层 — 多品种缓存共存 ⚠️ 高风险
**文件**：`xxybacktest/data.py`

**功能**：支持 stock 和 fund 的日线数据同时存在于 `_daily_cache` 中。

**矛盾点（严重）**：
- `preload_daily()` 中 `Data._daily_cache = cache`（第124行）——**覆盖赋值**
- `preload_fund_daily()` 中 `Data._daily_cache = cache`（第189行）——**覆盖赋值**
- 这意味着如果先加载 stock 再加载 fund，stock 的缓存会被 fund 完全覆盖！
- `get_daily_info()` 的 O1 缓存命中逻辑只查 `_daily_cache`，不会区分品种

**必须修改**：`preload_daily` 和 `preload_fund_daily` 需要从"覆盖"改为"合并追加"：
```python
# 改造前：Data._daily_cache = cache
# 改造后：
if Data._daily_cache is None:
    Data._daily_cache = {}
for date_str, day_data in cache.items():
    if date_str not in Data._daily_cache:
        Data._daily_cache[date_str] = {}
    Data._daily_cache[date_str].update(day_data)
```

**遗漏点**：`clear_cache()` 目前会清空所有缓存。多品种下是否需要在回测结束后保留？不需要，因为每次 `run_backtest` 是独立进程（ProcessPoolExecutor），但如果是单进程多账户测试，clear_cache 的时机需要调整。

---

### P1-S5：数据层 — 多品种懒加载
**文件**：`xxybacktest/data.py`

**功能**：
1. 新增 `Data._preload_flags = {"stock": False, "fund": False}`
2. 新增 `Data.ensure_preloaded(asset_type, start_date, end_date, calendar)`
3. `run_backtest` 中不再按 `asset_type` 二选一预加载，而是根据实际存在的账户动态加载

**矛盾点**：
- `Data.init_db(path, asset_type)` 中的 `asset_type` 参数用于设置 `Data._asset_type`，该变量在 `get_daily_info` 和 `get_dividend` 的兜底 SQL 查询中被使用
- 多品种下 `Data._asset_type` 是全局单值，无法同时表示 stock 和 fund
- 兜底查询需要按 code 后缀判断品种（`.OF`/`.SH` for fund, `.SZ`/`.SH` for stock），或让 `get_daily_info` 直接尝试两种表

**建议修改**：`Data.init_db` 不再接受 `asset_type`，`_asset_type` 标记废弃；兜底查询时优先查 daily_bar，无数据再查 daily_fund。

---

### P1-S6：交易函数增加 account_id ⚠️ 高风险
**文件**：`xxybacktest/trading.py`

**功能**：所有交易函数增加 `account_id=None` 参数：
```python
def order_buy(code, amount, context, account_id=None): ...
def order_sell(code, amount, context, account_id=None): ...
def order(security, amount, context, account_id=None): ...
def order_value(security, value, context, account_id=None): ...
def order_target_value(security, value, context, account_id=None): ...
def order_target_percent(security, percent, context, account_id=None): ...
def inout_cash(cash_amount, context, account_id=None): ...
def force_sell(code, context, account_id=None): ...
```

**矛盾点（严重）**：
1. `order_buy` / `order_sell` 内部需要同时访问：
   - `account.rule_list`（品种规则链）
   - `account.logs`（品种订单日志）
   - `context.portfolio`（统一资金池 + 统一持仓字典）
   
   这意味着结算逻辑需要拆成两部分：规则/日志走 account，资金/持仓走 context.portfolio。

2. `order_target_percent` 中 `target_value = context.portfolio.total_value * percent`，多账户下这个逻辑不变（因为共享资金池）。

3. `inout_cash` 原来是直接改 `context.portfolio.cash`，多账户下同样不变（共享资金池）。但如果以后要支持"从某账户出金"的语义，需要重新设计。

4. `force_sell` 被 `backtest.py` 的 `_morning_start` 调用，退市股/散股清仓时需要知道该持仓属于哪个 account 以记录到正确的日志中。

**遗漏点**：`force_sell` 需要传入 `account_id`，但 `_morning_start` 调用时需要从 `pos.account_id` 中读取。

---

### P1-S7：规则引擎 — 按账户隔离 ⚠️ 高风险
**文件**：`xxybacktest/rules.py`

**功能**：
1. `Rules.__init__` 增加 `account_id` 参数
2. `apply()` 中读取 `self.account.rule_list` 替代 `context.trade.rule_list`
3. `rule_cost` 中读取 `self.account.portfolio.cash` → 改为 `context.portfolio.cash`（因为资金共享）
4. `rule_volume_num` 中读取持仓时，需要确保查的是统一持仓池中的该 code

**矛盾点（严重）**：
- `rule_cost` 原来检查 `ctx.portfolio.cash >= total_needed`。多账户共享资金池下，这个逻辑**不需要改**，因为所有账户共享同一个 cash。
- 但 `rule_cost` 缩量的逻辑也应该保持：如果共享 cash 不够买某品种的股，就缩量。这是正确的行为。
- `rule_volume_num` 卖出时检查 `self.context.portfolio.positions[code]`，读取 `pos.enable_amount`。多账户下 enable_amount 在统一持仓中管理，所以**不需要改**。

**遗漏点**：无（ surprisingly 规则引擎的改动比预期小，因为资金和持仓都在共享池中）。

---

### P1-S8：回测主循环 — 内置事件多账户化 ⚠️ 高风险
**文件**：`xxybacktest/backtest.py`

**功能**：四个内置事件处理器从操作单账户改为遍历所有账户：

```python
def _before_market(ctx):
    # 1. 刷新所有持仓的 enable_amount（统一做）
    for code, pos in ctx.portfolio.positions.items():
        pos.enable_amount = pos.amount
    
    # 2. 送股处理：只处理 stock 账户的持仓
    for acc in ctx.accounts.values():
        if acc.asset_type != "stock":
            continue
        # 送股逻辑... 遍历 ctx.portfolio.positions，按 pos.account_id == acc.account_id 筛选
```

**矛盾点（严重）**：
1. `_morning_start` 退市股检查：需要遍历 `ctx.portfolio.positions`，对每个持仓判断其 `account_id` 对应的账户是否是 stock 类型，是才检查退市。
2. `_end_interval` 日终估值：统一刷新所有持仓市值，然后按 `pos.account_id` 归类记录到各 account 的 performance 中。
3. `_after_market` 分红登记与发放：
   - 登记阶段：需要按品种分别查分红（`get_dividend` vs `get_fund_dividend`），但查询结果写入共享的 `ctx.data.dividend`
   - 发放阶段：遍历 `ctx.portfolio.positions`，按 `pos.account_id` 找到对应 account，现金入账到 `ctx.portfolio.cash`（共享池）

4. **F3 预加载**（`backtest.py:325-367`）：目前按 `asset_type` 二选一执行预加载。多品种下需要为所有存在的 asset_type 各执行一次。

**遗漏点**：`backtest.py:152-183` `_before_market` 中送股处理修改 `pos.amount` 和 `pos.total_cost`，但 `pos.account_id` 是只读的，不需要额外处理。

---

### P1-S9：回测主循环 — 绑定函数签名更新
**文件**：`xxybacktest/backtest.py`

**功能**：`run_backtest` 中绑定到 context 的 lambda 需要增加 `account_id` 参数：

```python
# 改造前
context.order_target_percent = lambda security, percent: _order_target_percent(security, percent, context)

# 改造后
context.order_target_percent = lambda security, percent, account_id=None: _order_target_percent(security, percent, context, account_id)
```

**矛盾点**：`get_portfolio` / `get_account_positions` 也需要考虑多账户。目前这两个函数返回的是统一资金池的数据，所以不需要改。但未来如果要返回某 account 的独立视角，需要扩展。

**遗漏点**：无

---

### P1-S10：回测主循环 — 结果 DataFrame 改造
**文件**：`xxybacktest/backtest.py`

**功能**：
1. `context.order` DataFrame 目前只包含主账户订单。多账户下需要增加 `account_id` 列。
2. 新增 `context.orders_by_account = {account_id: df}` 供细粒度查看。
3. `context.pos` DataFrame 同理，增加 `account_id` 列。

**矛盾点**：现有 `simulation/runner.py:_save_results()` 直接读取 `context.order` 和 `context.pos`。改造后需要确保存储逻辑能处理新增的 `account_id` 列。

**遗漏点**：回测结束后，每个 account 的独立 `logs.order_list` 也需要提供 DataFrame 视图。

---

### P1-S11：绩效计算 — 组合级 + 分账户明细
**文件**：`xxybacktest/performance.py`

**功能**：
1. 组合级绩效（`context.performance`）保持现有逻辑不变
2. 分账户只记录：
   - 持仓明细（position_snapshots）
   - 持仓占组合比例（position_ratio）
   - 交易胜率（win / trade_num）
   - 订单列表
3. **不计算**分账户的独立年化/夏普/回撤（因为资金共享，无法分离）

**矛盾点**：
- `_compute_indicators` 中的 `roto`（总收益率）= `total_value / starting_cash - 1`，在共享资金池下依然正确。
- `SQN` 计算依赖 `trade_returns`，各 account 的 `trade_returns` 是独立的，可以分别计算各品种的 SQN。

**遗漏点**：如果用户需要"虚拟独立收益率"（按资金配额法），可以在 Phase 2 增强。

---

### P1-S12：`__init__.py` 导出新 API
**文件**：`xxybacktest/__init__.py`

**功能**：
```python
from .objects import Account  # 新增
# context.add_account 在运行时动态绑定，不需要静态导出
```

**矛盾点**：无

**遗漏点**：无

---

### P1-S13：`types.py` Protocol 更新
**文件**：`xxybacktest/types.py`

**功能**：更新 `Context` Protocol，增加：
```python
accounts: Dict[str, Any]  # Account 对象字典
main_account_id: str
add_account: Callable
get_account: Callable
```

**矛盾点**：Protocol 是静态类型提示，不影响运行时。但 IDE 补全和 mypy 检查会依赖此定义。

**遗漏点**：`Account` 类如果保持为普通类而非继承 DictObj，Protocol 中可以用 `Any` 或定义新的 `Account` Protocol。

---

### P2-S14：simulation/runner.py 多账户结果保存
**文件**：`xxybacktest/simulation/runner.py`

**功能**：
1. `_save_results` 需要处理 `context.accounts` 中各 account 的独立数据
2. 每日净值、持仓、订单都需要按 `account_id` 分别存储
3. 组合级数据单独存储（如 `aggregate_daily_values.parquet`）

**矛盾点（严重）**：
- `run_all()` 使用 `ProcessPoolExecutor` 并行执行多个 `run_single()`。每个 `run_single()` 调用一次 `run_backtest()`。
- 如果 `run_backtest()` 内部管理多账户，则 `run_single()` 返回的 context 包含多个 account 的数据。
- 但 `ProcessPoolExecutor` 的多进程模型意味着每个进程有自己的内存空间，数据不会共享。这是可以接受的。
- 问题：`simulation/submitter.py` 中一个"账户"是否允许关联多个"品种账户"？当前表结构可能不支持。

**建议**：Phase 2 保持 simulation 服务层不变，只在 `_save_results` 中增加按 account 拆分存储的逻辑。子账户配置放在 Phase 3。

---

### P2-S15：simulation/submitter.py 子账户配置
**文件**：`xxybacktest/simulation/submitter.py`

**功能**：考虑是否在账户配置中增加 `linked_accounts` 或 `sub_accounts` 字段。

**矛盾点**：当前 `simulation_accounts` 表（JSON 持久化）每行是一个账户。如果支持多品种，可能需要：
- 方案 A：一个账户一行，内部配置包含 `asset_type`（但这样就只能单品种）
- 方案 B：一个账户一行，策略代码内部通过 `add_account` 创建子账户（无需改 submitter）
- 方案 C：增加 `parent_account_id` 字段，支持主子账户关系

**建议**：Phase 2 采用方案 B（策略内部管理），不改 submitter。Phase 3 再考虑方案 C。

---

### P2-S16：实盘层 — live/context.py 多账户
**文件**：`xxybacktest/live/context.py`

**功能**：`create_live_context` 需要支持从多个 QMT 账户构建 context。

**矛盾点**：当前 `create_live_context(account, trader)` 只接收一个 trader。多品种下如果每个品种对应不同 QMT 资金账号：
```python
def create_live_context(main_account, main_trader, extra_accounts=None):
    # extra_accounts: [{"account_id": "fund", "trader": fund_trader, ...}]
```

**建议**：Phase 2 先支持单 QMT 账户下的多品种（同一个人可以在一个 QMT 账户里同时买卖股票和基金），多 QMT 账户放在 Phase 3。

---

### P2-S17：实盘层 — live/trading.py account_id
**文件**：`xxybacktest/live/trading.py`

**功能**：实盘交易函数增加 `account_id` 参数：
```python
def order_target_percent(security, percent, context, account_id=None): ...
```

**矛盾点**：实盘层的 `account_id` 需要映射到 QMT 的资金账号。如果单 QMT 账户支持多品种，则 `account_id` 只影响规则校验和日志记录，不影响下单通道（都用同一个 `context._trader`）。

**建议**：Phase 2 中 `account_id` 在实盘层只做日志归类，不改变下单通道。

---

### P2-S18：实盘层 — live/runner.py 绑定
**文件**：`xxybacktest/live/runner.py`

**功能**：绑定交易函数的 lambda 需要增加 `account_id` 参数：
```python
ctx.order_target_percent = lambda security, percent, account_id=None: _order_target_percent(security, percent, ctx, account_id)
```

**矛盾点**：无（与回测层对称）

---

### P2-S19：实盘层 — live/recorder.py 存储
**文件**：`xxybacktest/live/recorder.py`

**功能**：`_save_live_results` 需要按 account 分别存储订单和持仓。

**矛盾点**：当前存储路径为 `data/live/accounts/{account_id}/`。多品种下可以在该目录内增加品种子目录，或在文件名中体现品种。

**建议**：Phase 2 保持存储路径不变，在 Parquet 文件中增加 `account_id` 列即可。

---

### P3-S20：tests/ 测试用例更新
**文件**：`tests/` 目录下约 50 个测试文件

**功能**：
1. 新增多账户回测的集成测试
2. 验证 stock + fund 同时回测的场景
3. 验证资金共享是否正确（股票清仓后基金全仓）

**矛盾点**：大量测试文件引用 `context.portfolio`、`context.logs`、`context.performance` 等，需要确保向后兼容。

**建议**：优先跑通现有测试，确认无回归后再新增多账户测试。

---

### P3-S21：web/ API 路由扩展
**文件**：`web/routes/account.py`, `web/routes/api.py`, `web/routes/dashboard.py`

**功能**：
1. 净值曲线 API 增加 `account_id` 筛选参数
2. 持仓 API 增加 `account_id` 筛选参数
3. 订单 API 增加 `account_id` 筛选参数

**矛盾点**：当前 API 路径如 `/api/accounts/<account_id>/nav` 的 `account_id` 是 simulation 账户 ID。多品种下需要在查询参数中增加 `asset_account` 或 `sub_account` 参数。

**建议**：Phase 3 再处理 Web 层。

---

## 四、关键矛盾汇总（必须解决）

| # | 矛盾描述 | 涉及文件 | 解决方案 |
|---|---------|---------|---------|
| 1 | `Data._daily_cache` 被 `preload_daily` / `preload_fund_daily` 覆盖赋值 | `data.py` | 改为合并追加（`dict.update`） |
| 2 | `Data._asset_type` 是全局单值，无法同时表示 stock 和 fund | `data.py` | 废弃 `_asset_type`，兜底查询按 code 后缀或尝试双表 |
| 3 | `context.account`（费率配置）与新 `Account` 类命名冲突 | `context.py`, `objects.py` | Account 类改名为 `AssetAccount`，或 `context.account` 保持代理 |
| 4 | `simulation/runner.py` 的 `_save_results` 只处理单账户数据 | `simulation/runner.py` | 增加循环遍历 `context.accounts` 分别存储 |
| 5 | 事件处理器闭包直接操作 `context.portfolio`，需改为多账户遍历 | `backtest.py` | 闭包内部增加 `for acc in ctx.accounts.values()` 循环 |
| 6 | `rules.py` 的 `rule_cost` 和 `rule_volume_num` 需要知道当前订单属于哪个 account | `rules.py` | `Rules.__init__` 增加 `account_id` 参数 |
| 7 | `types.py` Protocol 定义单账户结构 | `types.py` | 增加 `accounts`、`add_account`、`get_account` 到 Context Protocol |
| 8 | `backtest.py` F3 预加载按 `asset_type` 二选一 | `backtest.py` | 改为遍历所有存在的 asset_type |
| 9 | `live/context.py` 只接收单个 trader | `live/context.py` | Phase 2 先保持单 trader，account_id 只做日志归类 |
| 10 | `force_sell` 在 `_morning_start` 中调用时，需要知道持仓属于哪个 account | `backtest.py`, `trading.py` | 从 `pos.account_id` 读取，传入 `force_sell` |

---

## 五、遗漏点汇总（方案未覆盖）

| # | 遗漏描述 | 涉及文件 | 建议 |
|---|---------|---------|------|
| 1 | Account 类需要支持序列化（`__getstate__` / `__setstate__`） | `objects.py` | 如果 Account 存到 `context.g` 中会被持久化，需确保可序列化 |
| 2 | `context.g` 的持久化（实盘状态保存）在多账户下如何处理 | `live/utils.py` | 目前只保存 `context.g`，不保存 `context.accounts`。如果策略状态依赖 accounts，需要扩展持久化逻辑 |
| 3 | `Data._dividend_reg_cache` 是否也需要合并追加（类似 `_daily_cache`） | `data.py` | `preload_dividend` 和 `preload_fund_dividend` 也使用覆盖赋值，需检查 |
| 4 | `run_backtest` 返回值中 `context.order` / `context.pos` 的 account_id 列 | `backtest.py` | 需在构建 DataFrame 时增加 account_id 列 |
| 5 | `simulation/runner.py` 的 `run_all` 并行模型是否兼容单 context 多账户 | `simulation/runner.py` | 兼容，因为每个进程独立运行 `run_backtest` |
| 6 | 回测结果中的 `context.performance.position_ratio` 目前只记录组合级，多账户下是否需要按 account 分别记录 | `performance.py` | 各 account 的 `performance.position_ratio` 已独立记录 |
| 7 | `order_target_percent` 中 `percent` 是基于组合总资产还是某 account 的虚拟资产 | `trading.py` | 基于组合总资产（共享资金池），这是设计意图 |
| 8 | 如果两个 account 同时持有同一只 code（如某股票同时出现在 stock 和 fund 的选股池中），Position 对象如何处理 | `trading.py` | 理论上不会，因为 stock 和 fund 的代码空间不重叠。如果真发生，需要让 Position 支持多 account_id |

---

## 六、实施顺序建议

```
P1 核心引擎（可独立验证）：
  S1 → S2 → S3 → S4 → S5 → S6 → S7 → S8 → S9 → S10 → S11 → S12 → S13
  
验证点：
  - stock + fund 同时回测不报错
  - 资金共享验证：股票清仓后基金全仓买入
  - 向后兼容：现有单账户策略零改动运行

P2 模拟/实盘适配（依赖 P1）：
  S14 → S15 → S16 → S17 → S18 → S19
  
验证点：
  - simulation runner 能正确保存多账户结果
  - 实盘单 trader 多品种正常执行

P3 外围更新（依赖 P1+P2）：
  S20 → S21
```

---

*本面板随实现进展更新，每完成一个步骤在此标记状态。*
