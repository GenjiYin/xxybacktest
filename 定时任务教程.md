# Plombery 使用教程

Plombery 是一个带有 Web UI 的 Python 定时任务框架，你可以用它来运行和监控各种定时脚本，并通过浏览器查看任务状态、日志和输出数据。

---

## 目录

1. [安装](#安装)
2. [核心概念](#核心概念)
3. [快速开始：第一个任务](#快速开始第一个任务)
4. [启动服务并查看 Web UI](#启动服务并查看-web-ui)
5. [定时触发器场景大全](#定时触发器场景大全)
6. [多任务 Pipeline](#多任务-pipeline)
7. [任务参数](#任务参数)
8. [在 Web UI 中手动触发任务](#在-web-ui-中手动触发任务)
9. [查看日志和输出数据](#查看日志和输出数据)

---

## 安装

```bash
pip install plombery
```

---

## 核心概念

| 概念 | 说明 |
|------|------|
| **Task（任务）** | 一个 Python 函数，是实际执行工作的最小单元 |
| **Pipeline（流水线）** | 由一个或多个 Task 组成的执行序列，Task 按顺序依次运行 |
| **Trigger（触发器）** | 定义 Pipeline 何时运行，可以是定时计划，也可以手动触发 |

一个 Pipeline 可以有多个 Trigger，比如同一个任务既可以每天运行，也可以每小时运行。

---

## 快速开始：第一个任务

新建一个文件 `main.py`：

```python
import uvicorn
from plombery import get_app, register_pipeline, task, get_logger, Trigger
from apscheduler.triggers.interval import IntervalTrigger

# 第一步：用 @task 装饰器定义一个任务函数
@task
async def hello_world():
    logger = get_logger()
    logger.info("Hello, World! 任务正在运行...")

# 第二步：注册 Pipeline，并绑定触发器
register_pipeline(
    id="my_first_pipeline",
    name="我的第一个任务",
    description="每 10 秒运行一次的示例任务",
    tasks=[hello_world],
    triggers=[
        Trigger(
            id="every_10_seconds",
            name="每10秒",
            schedule=IntervalTrigger(seconds=10),
        )
    ],
)

# 第三步：启动服务
if __name__ == "__main__":
    uvicorn.run("main:get_app", reload=True, factory=True)
```

---

## 启动服务并查看 Web UI

```bash
python main.py
```

启动后打开浏览器访问：

```
http://localhost:8000
```

你会看到 Plombery 的 Web 界面，左侧列出所有 Pipeline，点击进去可以看到：

- **触发器列表**：每个触发器的下次运行时间
- **运行历史**：每次运行的状态（成功/失败/运行中）
- **日志**：每次运行的详细日志输出
- **数据**：任务返回的数据（如果有）

---

## 定时触发器场景大全

Plombery 使用 APScheduler 的触发器，以下是常见场景：

### 场景一：每隔 N 秒/分钟/小时运行

```python
from apscheduler.triggers.interval import IntervalTrigger

# 每 30 秒运行一次
IntervalTrigger(seconds=30)

# 每 5 分钟运行一次
IntervalTrigger(minutes=5)

# 每 2 小时运行一次
IntervalTrigger(hours=2)

# 每 1 天运行一次
IntervalTrigger(days=1)

# 每 1 周运行一次
IntervalTrigger(weeks=1)

# 组合：每 1 小时 30 分钟运行一次
IntervalTrigger(hours=1, minutes=30)
```

### 场景二：每天固定时间运行（Cron 风格）

```python
from apscheduler.triggers.cron import CronTrigger

# 每天早上 8:00 运行
CronTrigger(hour=8, minute=0)

# 每天晚上 23:30 运行
CronTrigger(hour=23, minute=30)

# 每天中午 12:00，指定时区
CronTrigger(hour=12, minute=0, timezone="Asia/Shanghai")
```

### 场景三：每周固定某天运行

```python
from apscheduler.triggers.cron import CronTrigger

# 每周一早上 9:00 运行
CronTrigger(day_of_week="mon", hour=9, minute=0)

# 每周一、三、五早上 9:00 运行
CronTrigger(day_of_week="mon,wed,fri", hour=9, minute=0)

# 每周末（周六、周日）运行
CronTrigger(day_of_week="sat,sun", hour=10, minute=0)
```

### 场景四：每月固定日期运行

```python
from apscheduler.triggers.cron import CronTrigger

# 每月 1 号凌晨 0:00 运行
CronTrigger(day=1, hour=0, minute=0)

# 每月最后一天运行
CronTrigger(day="last", hour=23, minute=59)

# 每月 1 号和 15 号运行
CronTrigger(day="1,15", hour=9, minute=0)
```

### 场景五：只运行一次（指定时间点）

```python
from apscheduler.triggers.date import DateTrigger
from datetime import datetime

# 在指定时间点运行一次
DateTrigger(run_date=datetime(2026, 6, 1, 10, 0, 0))
```

### 场景六：指定开始时间的间隔任务

```python
from apscheduler.triggers.interval import IntervalTrigger
from datetime import datetime
from dateutil import tz

# 从 2026-01-01 22:30（北京时间）开始，每天运行一次
IntervalTrigger(
    days=1,
    start_date=datetime(2026, 1, 1, 22, 30, tzinfo=tz.gettz("Asia/Shanghai")),
)
```

### 完整示例：一个 Pipeline 绑定多个触发器

```python
from apscheduler.triggers.interval import IntervalTrigger
from apscheduler.triggers.cron import CronTrigger
from plombery import register_pipeline, task, Trigger, get_logger

@task
async def backup_database():
    logger = get_logger()
    logger.info("开始备份数据库...")
    # 你的备份逻辑
    logger.info("备份完成")

register_pipeline(
    id="backup_pipeline",
    name="数据库备份",
    tasks=[backup_database],
    triggers=[
        Trigger(
            id="every_hour",
            name="每小时备份",
            schedule=IntervalTrigger(hours=1),
        ),
        Trigger(
            id="daily_midnight",
            name="每天凌晨完整备份",
            schedule=CronTrigger(hour=0, minute=0, timezone="Asia/Shanghai"),
        ),
    ],
)
```

---

## 多任务 Pipeline

一个 Pipeline 可以包含多个 Task，它们会按顺序依次执行：

```python
from plombery import register_pipeline, task, Trigger, get_logger
from apscheduler.triggers.cron import CronTrigger

@task
async def fetch_data():
    logger = get_logger()
    logger.info("第一步：获取数据")
    return {"records": [1, 2, 3]}  # 返回值会保存，可在 Web UI 中查看

@task
async def process_data():
    logger = get_logger()
    logger.info("第二步：处理数据")

@task
async def send_report():
    logger = get_logger()
    logger.info("第三步：发送报告")

register_pipeline(
    id="etl_pipeline",
    name="数据处理流水线",
    description="获取 -> 处理 -> 发送报告",
    tasks=[fetch_data, process_data, send_report],  # 按顺序执行
    triggers=[
        Trigger(
            id="daily_9am",
            name="每天早上9点",
            schedule=CronTrigger(hour=9, minute=0, timezone="Asia/Shanghai"),
        )
    ],
)
```

---

## 任务参数

你可以用 Pydantic 定义参数，在 Web UI 中手动触发时可以填写这些参数：

```python
from pydantic import BaseModel, Field
from typing import Optional
from plombery import register_pipeline, task, Trigger, get_logger
from apscheduler.triggers.interval import IntervalTrigger

class MyParams(BaseModel):
    target_url: str = Field(description="要检查的网址")
    timeout: int = Field(default=30, ge=1, le=120, description="超时秒数")
    notify_email: Optional[str] = None

@task
async def check_website(params: MyParams):
    logger = get_logger()
    logger.info(f"正在检查: {params.target_url}，超时: {params.timeout}s")
    # 你的逻辑...

register_pipeline(
    id="website_monitor",
    name="网站监控",
    tasks=[check_website],
    params=MyParams,  # 声明参数类型
    triggers=[
        Trigger(
            id="every_5min",
            name="每5分钟",
            # 触发器可以预设参数值
            params=MyParams(target_url="https://example.com", timeout=10),
            schedule=IntervalTrigger(minutes=5),
        )
    ],
)
```

---

## 在 Web UI 中手动触发任务

1. 打开 `http://localhost:8000`
2. 在左侧点击你的 Pipeline 名称
3. 点击右上角的 **Run** 按钮
4. 如果 Pipeline 有参数，会弹出表单让你填写
5. 点击确认后任务立即开始运行

---

## 查看日志和输出数据

在 Web UI 中：

1. 点击左侧 Pipeline → 选择一个触发器 → 点击某次运行记录
2. **Logs 标签**：查看 `logger.info/debug/warning/error` 输出的所有日志
3. **Data 标签**：如果任务函数有返回值（dict、list、pandas DataFrame），会在这里以表格形式展示

```python
@task
async def my_task():
    logger = get_logger()
    logger.info("这条会出现在 Logs 标签")
    logger.warning("这是一条警告")
    logger.error("这是一条错误")

    # 返回的数据会出现在 Data 标签
    return [
        {"name": "Alice", "score": 95},
        {"name": "Bob", "score": 87},
    ]
```

---

## 项目结构建议

当任务变多时，建议按如下结构组织代码：

```
my_project/
├── main.py              # 入口文件，启动服务
├── pipelines/
│   ├── __init__.py
│   ├── backup.py        # 备份相关任务
│   ├── monitor.py       # 监控相关任务
│   └── report.py        # 报表相关任务
└── .env                 # 环境变量（可选）
```

`main.py` 中导入所有 pipeline 模块即可：

```python
import uvicorn
from plombery import get_app  # noqa: F401

from pipelines import backup, monitor, report  # noqa: F401

if __name__ == "__main__":
    uvicorn.run("main:get_app", reload=True, factory=True)
```
